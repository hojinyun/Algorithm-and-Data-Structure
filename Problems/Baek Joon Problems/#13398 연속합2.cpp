#include <stdio.h>

int 
int dp(int numbers[n]){
	if (dp[i-1]
	for(int i = 2; i < n, i++){
		return dp[i] = dp[i]+dp[i+1];
	}
}

int main(void){
	int n;
	scanf("%d", &n);
	int numbers[n];
	for(int i = 0; i < n; i++){
		scanf("%d", &numbers[i]);
	}
	
	if
}
